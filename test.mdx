# Component

What should component have?

- `?.is(node)` - identify if DOM node is the node
- `?.merge(node1, node2)`
- `?.split(range)` - split DOM node
- `?.slots` - identify slots for a component: one or many elements
- `shortcuts | commands`

# Renderer library

It can render and cache elements. And allow pass plugins

# Editor library

# Components library
