{
  "name": "@brickifyio/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^1.13.3"
  }
}
